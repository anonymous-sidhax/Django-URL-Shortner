
    def get_url():
        res = set()
        for i in range(pow(63, 3)):
            n = i
            curr = ""
            for j in range(3):
                curr += USED_FOR_MAPPING[n % 64]
                n //= 64
            res.add(curr)

        return res

    res = get_url()
    for it in res:
        new_key = Keys(key=it)
        new_key.save()